#include <stdlib.h>

int main(void)
{
    realloc(NULL, 1000);
    realloc(NULL, 1000);
    realloc(NULL, 1000);
    realloc(NULL, 1000);
    realloc(NULL, 1000);
    realloc(NULL, 1000);
    return 0;
}
